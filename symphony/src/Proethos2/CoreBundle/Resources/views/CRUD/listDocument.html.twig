{% extends "::base.html.twig" %}

{% block title %}{% trans %}Documents{% endtrans %}{% endblock %}

{% block content %}
    
    <div class="container-fluid main-content download-docs">

        <div class="row">
            <div class='col-md-12'>
                <h1 class="page-header">{% trans %}Documents{% endtrans %}</h1>
            </div>
        </div>

        <div class="block">
            
            <div class="row">
{#
                {% for role in roles %}
                    {% if role in app.user.proethos2Roles %}
                        <div class="col-md-6">
                            <h2 class='sub-header'>{{ role.name|trans }}</h2>
                            <ul>
                                {% for document in documents %}
                                    {% if role in document.roles %}
                                        <li><a href="{{ document.uri }}">
                                            <span data-toggle="tooltip" data-placement="top" title="{{ document.description }}">
                                                {{ document.title }}
                                            </span>
                                        </a></li>
                                    {% endif %}
                                {% endfor %}
                            </ul>
                        </div>
                    {% endif %}
                {% endfor %}
#}
                {% if 'investigator' in app.user.rolesSlug %}
                    <div class="col-md-6">
                        <h2 class='sub-header'>{% trans %}Editor{% endtrans %}</h2>
                        <ul>
                            {% for document in documents %}
                                {% if 'investigator' in document.roles %}
                                    <li><a href="{{ document.uri }}">
                                        <span data-toggle="tooltip" data-placement="top" title="{{ document.description }}">
                                            {{ document.title }}
                                        </span>
                                    </a></li>
                                {% endif %}
                            {% endfor %}
                        </ul>
                    </div>
                {% endif %}

                {% if 'secretary' in app.user.rolesSlug %}
                <div class="col-md-6">
                    <h2 class='sub-header'>{% trans %}Secretary{% endtrans %}</h2>
                    <ul>
                        {% for document in documents %}
                            {% if 'secretary' in document.roles %}
                                <li><a href="{{ document.uri }}">
                                    <span data-toggle="tooltip" data-placement="top" title="{{ document.description }}">
                                        {{ document.title }}
                                    </span>
                                </a></li>
                            {% endif %}
                        {% endfor %}
                    </ul>
                </div>
                {% endif %}

                {% if 'member-of-committee' in app.user.rolesSlug %}
                <div class="col-md-6">
                    <h2 class='sub-header'>{% trans %}Member of Committee{% endtrans %}</h2>
                    <ul>
                        {% for document in documents %}
                            {% if 'member-of-committee' in document.roles %}
                                <li><a href="{{ document.uri }}">
                                    <span data-toggle="tooltip" data-placement="top" title="{{ document.description }}">
                                        {{ document.title }}
                                    </span>
                                </a></li>
                            {% endif %}
                        {% endfor %}
                    </ul>
                </div>
                {% endif %}

                {% if 'member-ad-hoc' in app.user.rolesSlug %}
                <div class="col-md-6">
                    <h2 class='sub-header'>{% trans %}Member ad-hoc{% endtrans %}</h2>
                    <ul>
                        {% for document in documents %}
                            {% if 'member-ad-hoc' in document.roles %}
                                <li><a href="{{ document.uri }}">
                                    <span data-toggle="tooltip" data-placement="top" title="{{ document.description }}">
                                        {{ document.title }}
                                    </span>
                                </a></li>
                            {% endif %}
                        {% endfor %}
                    </ul>
                </div>
                {% endif %}
                
                {% if 'administrator' in app.user.rolesSlug %}
                <div class="col-md-6">
                    <h2 class='sub-header'>{% trans %}Administrator{% endtrans %}</h2>
                    <ul>
                        {% for document in documents %}
                            {% if 'administrator' in document.roles %}
                                <li><a href="{{ document.uri }}">
                                    <span data-toggle="tooltip" data-placement="top" title="{{ document.description }}">
                                        {{ document.title }}
                                    </span>
                                </a></li>
                            {% endif %}
                        {% endfor %}
                    </ul>
                </div>
                {% endif %}
            </div>

        </div>

    </div>
{% endblock %}

{% block script %}
    <script>
        $(function(){

        });
    </script>
{% endblock %}
